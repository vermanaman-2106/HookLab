export const metadata = {
  title: "Studio — HookLab AI",
  description: "Chat with HookLab to turn ideas into hooks, scripts, and strategy.",
};

export default function AppLayout({ children }) {
  return children;
}
